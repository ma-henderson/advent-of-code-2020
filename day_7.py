with open("day_7_inputs", 'r') as f:
  nodes = []
  for line in f:
    # Find all the nodes in which a shiny gold bag can be carried in

    # Sentence structures include:
    # Direct (without commas)
    if line.count(",") == 0:
      # Identify the in
      

    # Connection (with commas)